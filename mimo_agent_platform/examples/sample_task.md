# Sample Task

任务示例：

> 为现有 Python 服务补充统一错误处理中间件，要求兼容已有接口返回结构，并给出测试与回滚建议。

推荐运行方式：

```powershell
python -m mimo_agent_platform.cli `
  --task "为现有 Python 服务补充统一错误处理中间件，要求兼容已有接口返回结构，并给出测试与回滚建议" `
  --repo . `
  --provider mock `
  --output sample_run.json
```
